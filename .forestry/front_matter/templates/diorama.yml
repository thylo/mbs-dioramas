---
label: Diorama
hide_body: true
fields:
- name: name
  type: text
  config:
    required: false
  label: Name
  description: Name of the the diorama
- name: slug
  type: select
  default: fields
  config:
    required: true
    options:
    - fields
    - cliffs
    - dunes
    - seaside
    - mudflats
    - swamp
    source:
      type: simple
      section: 
      file: 
      path: 
  label: slug
  description: Reference to one diorama
